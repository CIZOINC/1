'use strict';

var gulp = require('gulp'),
    browserSync = require('browser-sync');

var paths = {
    source: {
        js: ''
    },
    dest: {
        prod: '',
        serve: ''
    }
};

// Clean files task

// Watch task

// Browsersync task
